name: 'Terraform GitHub Actions'
on:
  - push

jobs:
  terraform:
    name: 'Terraform'
    runs-on: ubuntu-latest
    steps:

      - name: 'Checkout'
        uses: actions/checkout@master

      - name: 'Terraform Format fmt'
        uses: clouddrove/github-actions@master
        with:
          tf_actions_version: 0.13.0
          actions_subcommand: 'fmt'
          tf_actions_working_dir: "./_example"
          tf_actions_comment: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}


      - name: 'Terraform Init'
        uses: clouddrove/github-actions@master
        with:
          tf_actions_version: 0.13.0
          actions_subcommand: 'init'
          tf_actions_working_dir: "./_example"
          tf_actions_comment: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure AWS Credentials
        uses: clouddrove/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-2

      - name: 'Terratest'
        uses: clouddrove/github-actions@v3.0
        with:
          tf_actions_subcommand: 'terratest'
          tf_actions_working_dir: ./_test
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: 'Slack Notification'
        uses: clouddrove/action-slack@v2
        with:
          status: ${{ job.status }}
          fields: repo,author
          author_name: 'Clouddrove'
        env:
          GITHUB_TOKEN: ${{ secrets.GITOKEN }} # required
          SLACK_WEBHOOK_URL: ${{ secrets.SLAK }} # required
        if: always()